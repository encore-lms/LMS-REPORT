# 발행 HTML 은 sections/ 조각에서 조립하는 생성물이다.
# 저장소에 두면 어느 섹션을 고치든 이 파일이 함께 바뀌어 공동 작업 시 충돌한다.
# 배포는 pages.yml 이 build.py 를 돌려 만든다. 로컬 확인은 `python3 build.py` 후 진행.
2026-08-05_임원보고_진행현황.html

# 로컬 미리보기 산출물
_site/

.DS_Store
